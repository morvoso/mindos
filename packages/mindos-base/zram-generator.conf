# MindOS: compressed RAM swap instead of a swap partition. lz4 compresses a
# little less than zstd but costs a fraction of the CPU per page when the
# swap is actually used.
[zram0]
zram-size = min(ram / 2, 16384)
compression-algorithm = lz4
swap-priority = 100
