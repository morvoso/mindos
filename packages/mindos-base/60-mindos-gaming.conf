# MindOS gaming/development kernel tuning. Unknown keys (e.g. on a foreign
# kernel without BORE) only produce a warning.

# Proton/Wine games map a lot of memory; Steam recommends this.
vm.max_map_count = 2147483642
# zram-backed swap: swap eagerly, no readahead.
vm.swappiness = 100
vm.page-cluster = 0
# Bound dirty page cache so big writes do not stall the game.
vm.dirty_bytes = 268435456
vm.dirty_background_bytes = 67108864
# Some games trigger split locks; the default mitigation freezes them for 10ms.
kernel.split_lock_mitigate = 0
kernel.nmi_watchdog = 0
kernel.sched_bore = 1
fs.file-max = 2097152
fs.inotify.max_user_watches = 524288
fs.inotify.max_user_instances = 8192
net.core.default_qdisc = fq
net.ipv4.tcp_congestion_control = bbr
net.ipv4.tcp_fastopen = 3
