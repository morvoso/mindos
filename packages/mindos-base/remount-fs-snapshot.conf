# A snapshot booted from the MindOS boot menu runs on a RAM overlay
# (systemd.volatile=overlay); remounting / from fstab makes no sense there.
[Unit]
ConditionKernelCommandLine=!systemd.volatile=overlay
